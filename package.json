{
  "name": "taskmaster",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "dev:server": "pnpm --filter server dev",
    "dev:client": "pnpm --filter web-client dev",
    "dev:ds": "pnpm --filter design-system run storybook",
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "server:generate-resource": "pnpm --filter server generate:resource",
    "server:add": "pnpm --filter server add",
    "client:add": "pnpm --filter web-client add",
    "ds:add": "pnpm --filter design-system add",
    "ds:build": "pnpm --filter design-system build",
    "server": "pnpm --filter server",
    "client": "pnpm --filter web-client",
    "ds": "pnpm --filter design-system"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {}
}
