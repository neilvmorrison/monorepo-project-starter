{
  "name": "your-project",
  "private": true,
  "scripts": {
    "dev:backend": "pnpm --filter backend dev",
    "dev:frontend": "pnpm --filter frontend dev",
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "backend:generate-resource": "pnpm --filter backend generate:resource",
    "backend:add": "pnpm --filter backend add",
    "frontend:add": "pnpm --filter frontend add"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {}
}
